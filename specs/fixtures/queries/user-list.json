{
  "user": {
    "id": true,
    "name": true
  },
  "$list": {
    "limit": 10,
    "orderBy": [{ "field": "name", "dir": "asc" }],
    "filter": [{ "field": "role", "op": "eq", "value": "admin" }]
  }
}
