{
  "user": {
    "$select": {
      "id": true,
      "name": true
    },
    "$page": { "first": 10 },
    "$orderBy": [{ "field": "name", "direction": "asc" }],
    "$where": { "field": "role", "op": "eq", "value": "admin" }
  }
}
