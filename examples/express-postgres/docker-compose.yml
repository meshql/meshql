services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: meshql
      POSTGRES_PASSWORD: meshql
      POSTGRES_DB: meshql
    ports:
      - "5432:5432"
    volumes:
      - meshql_pg:/var/lib/postgresql/data

volumes:
  meshql_pg:
