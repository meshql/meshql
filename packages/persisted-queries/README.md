# meshql-persisted-queries

Register MeshQL queries and send `X-Mesh-Query-Id` instead of full base64 headers.

See [@meshql/persisted-queries on JSR](https://jsr.io/@meshql/persisted-queries) and [docs](https://docs.meshql.dev/packages/persisted-queries).
